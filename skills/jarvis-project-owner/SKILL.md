---
name: jarvis-project-owner
description: Accept and coordinate project-scoped Jarvis work after validating owner, project, and permission boundaries.
---

# Jarvis Project Owner

Validate that the dispatch targets this project and role. A valid owner may execute project-local work while Main remains routing-only. Preserve unrelated worktree changes, use selective staging, and report blockers to the configured parent instead of asking unrelated users.

Completion requires a durable report plus an actual parent feedback receipt. Use `jarvis reports finalize-feedback` only after delivery succeeds.
