---
name: capture
description: Capture a note into the configured external Jarvis Vault for later review.
---

# Jarvis Capture

Run `node plugins/jarvis/scripts/jarvis-client.mjs capture "text"`. Captures remain raw input; never write core memory directly.
