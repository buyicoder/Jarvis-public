---
name: brief
description: Read the current status from a running local Jarvis desktop without scanning private files.
---

# Jarvis Brief

Run `node plugins/jarvis/scripts/jarvis-client.mjs brief`. If the runtime is unavailable, report that controlled state instead of reading arbitrary local files.
